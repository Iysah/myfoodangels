import { store } from "../../../store/root";

interface aboutParam {
    about: string;
}

interface bioParams {
    name: string;
    skill: string;
    position: string;
    country: string;
    city: string;
}

interface achievementsParams {
    title: string;
    sport: string;
    date: string;
    description: string;
}

